# 新样本回归重建 To Do（2026-07-02）

前提（已完成/已拍板）：

- 事件样本 = `new data set/processed/final_event_sample_main.csv`，**125 事件**（134 减去 9 条无日度官方日期的，剔除规则已固化在 `build_event_dates.py`，剔除名单在 `final_sample_dropped_no_day_date.csv`）。
- 公司池**不沿用**旧 86 家，重新定义。
- 旧面板（60 事件 × 86 公司）的 CAR 和媒体情绪是外部 Excel 交付的成品，无法复用；新旧事件仅 26 个可对上，本轮按全部重建处理。

责任标注：【你】= 需要你拉数据或拍板；【C】= Claude 项目内完成；【你+C】= C 初筛、你终审。

---

## T1. 公司池重建 【你+C】 ←当前阻塞点，先做

1. C 从 125 事件的厂商清单出发，起草可复现的入池规则（候选维度：模型厂商上市主体/控股股东、云与算力供应链、芯片、应用层、直接竞品），生成候选公司名单初稿。
2. 你终审名单和规则。
3. 产出：`new data set/decisions/firm_universe_decisions.csv`（逐公司：ticker、入池理由、维度标签）+ 规则说明。约定同 entity/date 两张决策表：**决策表是唯一真值**。

依赖：无。T3/T4/T6 的范围都由它决定。

## T2. 面板骨架 + 拉数清单 【C】

1. 从 official_date 推导"美东首个可反应交易日"（美盘中/盘前发布取当日，盘后取次一交易日，周末与休市顺延），规则脚本化。
2. 生成 125 事件 × N 公司骨架（事件字段、日期、AA 指标、事件标签就位，CAR/财务留空）。
3. 给你一份精确拉数清单：每家公司需要的日收益区间、财务季度区间。

依赖：T1。

## T3. 日收益 + 因子数据 【你】

- N 家公司日收益（复权），**2021-07 ～ 2026-04**（给 2022-04 最早事件留估计窗）。
- 同期 FF3 日因子 + 市场指数收益。（波动率、动量不用你拉，C 从日收益算。）
- 原始文件直接进 `data/raw/returns/`，格式按 T2 给出的约定；来源和拉取日期写进同目录 README。

依赖：T2 的清单。

## T4. 季度财务 【你】

- N 家公司 **2022Q1 ～ 2026Q1**：总资产、所有者权益账面值、市值（算 size_log_assets 和 bm_ratio 用）。
- 进 `data/raw/fundamentals/`，同样附来源说明。

依赖：T2 的清单。

## T5. CAR 计算脚本化 【C】

- 市场模型 + FF3，8 个窗口（pre/1/2/3/5/10/15/20），估计窗与剔除规则写死在脚本里，进 `scripts/prep/`。
- 这次 CAR 必须由脚本从原始收益算出，不再接受成品交付——这是旧链路不可复现的根源。

依赖：T3。

## T6. 关系编码 【你+C】

- 125 事件 × N 公司的 event-firm 关系（publisher/供应链/竞品/无关…），复用既有 coder A/B + 仲裁流程文档。
- C 跑自动初筛 + 双编码，分歧表交你仲裁。
- 产出：新版 `relationship` 决策表（AIT-* 事件 ID 体系），进 `new data set/decisions/`。

依赖：T1；可与 T3/T4 并行。

## T7. 媒体情绪 【你拍板 + 你生产】

- 先决定：保留还是砍掉媒体情绪变量。
- 保留的话，至少 108 个新事件（含 2022-2023 的 ChatGPT、GPT-4、Llama 2 等大事件）需要媒体文本和情绪打分，生产链在你那边。
- 建议：不阻塞主回归，先跑无媒体版本，媒体作为二期后补。

依赖：独立，不阻塞其他任务。

## T8. 事件标签 + AA 指标对接 【C】

- is_reasoning / is_open_weight / is_chinese / 模态等标签对 125 事件重打（从 AA 数据和事件文本半自动生成，存疑条目列表交你确认）。
- 代表模型 AA 指标（intelligence index、价格、速度等）从 `new data set/raw/aa_api_v2` 快照对接。

依赖：无，可立即开始。

## T9. 合并 + 回归重跑 【C】

- 合并 T2-T8 成新面板 `clean_event_firm_panel_v2.csv`。
- 重跑：core_table（主回归）、heterogeneity、CAR_pre 控制（注意 [[project-car-pre-attenuation]] 的既有发现）、specr 稳健性、quarter_pricing_regime。
- 出一份新旧样本结果对比报告：主效应方向/显著性是否稳、哪些异质性结论变了。

依赖：T3、T4、T5、T6 完成。

---

## 关键路径

T1（公司池）→ T2（骨架+清单）→ 你拉 T3/T4 → T5（CAR）→ T9（回归）。
T6、T8 并行插空；T7 二期。

你下一步只有一件事：**T1 终审公司池**。C 可以现在就把候选名单初稿做出来。
