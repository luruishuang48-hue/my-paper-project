# Multi-Agent Execution Plan

任务：把基于旧数据的文件、旧脚本和旧中间产物归档到根目录 `old/`，保留新数据链路。

时间：20260703-151110，北京时间。

状态：已完成执行和审阅。

## 规划阶段

1. 已完成。盘点根目录、`data/`、`task/`、`scripts/`、`reports/`、`agent_tasks/`、`archive/` 和嵌套同名项目目录。
2. 已完成。识别新数据链路的必留文件。核心包括 `new data set/`、`Analysis/`、`CAR/`、`Fundamentals/`、`事件标签/`、`关系标签/`、`企业列表/` 和当前任务工作区。
3. 已完成。识别旧数据链路候选。重点包括旧事件集 CSV、旧关系表、旧 relationship coding 任务输出、旧 source reports、旧脚本和旧分析报告。
4. 已完成。检查候选文件是否仍被新脚本或当前文档引用。被当前标签流程作为规则来源的旧 codebook 未移动。

## 实施阶段

1. 已完成。建立 `old/` 目录，按原相对路径迁移确认旧内容。
2. 已完成。对明显重复的嵌套旧项目目录整体归档。
3. 已完成。保留移动清单、跳过清单和风险备注。

## 审阅阶段

1. 已完成。校验新数据目录、标签目录、面板目录仍存在。
2. 已完成。搜索旧路径引用，区分历史引用和仍会破坏运行的引用。
3. 已完成。运行轻量结构检查，确认被归档文件不再散落在根目录关键位置。

## 修订阶段

1. 已完成。根据审阅结果补移遗漏的旧任务记录、旧稿修改意见、`Tex/` 旧稿和 `old60` 对照产物。
2. 已完成。更新归档清单。
3. 已完成。输出最终说明，列明移动范围、保留范围和后续建议。
