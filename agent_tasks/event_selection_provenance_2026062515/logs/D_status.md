# D 任务状态日志

日期：2026-06-25

## 已完成
1. 用 `gb18030` 读取主文件 `archive/old_relationship_20260625/task/事件集数据.csv`（5,162 行 × 106 列），
   `-new.csv` 内容一致；用 `utf-8` 读取 `-relationships.csv`（5,161×84）与 `-relationships_v2.csv`（5,160×94）。
2. 确认文件演化关系：106 列版本（主文件/`-new.csv`，Jun 1）是事件级人工标注阶段产物；
   84/94 列的 `-relationships(_v2).csv`（Jun 11 / Jun 25）是在 60 事件已固定后，为公司-事件关系
   异质性分析追加列的下游衍生版本，不是更早的筛选草稿。
3. 发现并清洗了主文件中的"表头污染行"（事件 ID 字面值为 `final_event_id` 的伪行 + 1 条全 NaN 空行），
   去除后事件级唯一 ID 精确为 60。
4. 核心发现：`是否可进入 LLM 能力机制样本`(47) 与 `是否可进入 media 模型能力补充样本`(13) 是**互斥二分类**，
   并集=交集为空=60，这是子样本划分而非筛选。13 个 media 事件逐一核对均为图像/视频生成模型
   （Imagen、SORA、Veo、Kling、Stable Diffusion 3 等）。
5. Tier 分层、美股相关性、ticker 缺失、中国发布者标记，均逐一交叉验证后确认**对最终样本筛选无区分度**
   （诚实排除了这些猜测）。
6. `合并规则` + `合并事件包含的原始模型行数量` 解释了 71 条原始模型行 → 60 个事件（11 行通过 9 条
   家族合并规则被吸收）。
7. `模型选择规则说明` 列核查：60 行全部是同一固定文本（关于"代表行选取规则"，非逐事件筛选备注），
   没有发现用户记忆中可能存在的个性化筛选文字依据。
8. 用 pandas 精确比对 `事件 ID`（清洗后）vs `最终面板 final_event_id`：**60/60 完全一致，零差异**，
   同时 `-relationships.csv` 与 `-relationships_v2.csv` 的 60 个事件 ID 也与之完全一致。
   确认本文件链路是最终面板的直接唯一上游。

## 产出
- `agent_tasks/event_selection_provenance_2026062515/outputs/D_event_data_manual_annotation_columns.md`

## 未解决/移交
- 266 → ~71 这一段的具体逐条筛选记录仍未找到，本文件只能验证到 71 这个中间起点。
