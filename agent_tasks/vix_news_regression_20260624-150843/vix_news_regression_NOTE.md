Not migrated: `vix_news_regression.R` does use relationship dummies (`owner`, `investor`, `competitor`, `upstream`, `downstream`, `business_upstream`, `real_upstream`, `business_downstream`, `real_downstream`) as regressors/subsample filters in Part 2, but these are built independently in `prep_621.py` by parsing the `relationship` string column from the raw file `data/relationships/6.21事件集数据.csv` — the script never reads or depends on `data/panel/specr_rel_clean.csv` (confirmed via grep: zero references). Since the OLD→NEW column migration applies specifically to `specr_rel_clean.csv`, it is not applicable to this script; no `_v2` was created.
