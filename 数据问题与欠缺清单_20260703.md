# 数据问题与欠缺清单（2026-07-03）

对 `Analysis/processed/event_firm_panel.csv`（13,500 行 = 125 事件 × 108 公司，232 列）
及其上游全部数据的审阅汇总。本文件是当前唯一的问题清单，覆盖此前散落在对话与各
报告中的所有已提问题；每条标了严重级别、状态和责任人。配套任务排期见
`to_do_rebuild_regression_20260702.md`。

---

## 〇、已验证通过的部分（抽验记录，供审稿备查）

| 检查项 | 结果 |
|---|---|
| 面板结构 | 13,500 行无重复键，125 事件 × 108 公司完整交叉 |
| CAR 独立复算 | DeepSeek R1 × NVDA：alpha/beta/CAR[0,5]/CAR[0,20] 六位小数完全一致 |
| 经济方向 | R1 事件 NVDA CAR[0,5] = −5.2%（1/27 暴跌落在 t+4，被捕捉） |
| 价格数据 | 148,125 行，零重复、对照 SPY 日历零缺日；NVDA 拆股连续；9 个极端收益均为真实事件 |
| FF3 因子 | 覆盖 2021-01-04 ~ 2026-04-30，与价格对齐，百分比/小数双版本 |
| 事件日期 | 与流水线 125 事件主样本零偏差；4 条节假日滚动（感恩节/MLK 等)正确 |
| ADR 换算 | TSM 5:1、ASX 2:1、UMC 5:1 股数口径正确，市值合理 |
| 财务准确性 | NVDA/MSFT/TSM 抽查与 10-Q/年报一致；总资产零缺失 |
| beta 分布 | 中位数 0.87，p95=2.06；近零 beta 全是公用事业/必需消费（EXC、PEP、KHC），合理 |
| 缺失 CAR | 327 行全部来自 8 家晚上市公司（CRWV/SNDK/NBIS/ALAB/ARM 等），有据可查 |

日期端历史问题（已关闭）：134 事件全部标注日度日期后，Claude 3.5 Sonnet 的 UTC
时区伪影已改（06-21→06-20），Imagen 3 / Flash-8B / Claude 3.5 Haiku 三条错配已换
正确来源，9 条无日度日期事件已按你的决定剔除（规则固化在 `build_event_dates.py`）。

---

## 一、必须修复才能跑回归的问题

### P1. 财务合并是纯前视匹配 —— ✅ 已修复（2026-07-03）
- **原问题**：按"事件所在日历季度"合并，86% 行的财政期末在事件日之后。
- **修复**：`build_event_firm_panel.py` 改为滞后 as-of 匹配（可用日 = period_end
  + 45 天，取事件日前最近一期，报表超过 400 天置缺失）。重建后 **0 行违规**，
  报表年龄中位数 87 天，新增 `fund_staleness_days` 诊断列；半年报公司反而多配上了
  51 行。CAR 复核未受影响（NVDA×R1 七位小数一致）。

### P2. 负 B/M 未处理 —— ✅ 已修复（2026-07-03）
- 负权益行 `bm_ratio` 置缺失（剩余 0 行为负），`negative_equity` dummy 711 行，
  原值保留在 `bm_ratio_raw`。回归阶段的 winsorize 待 T9。

### P3. 回归核心变量缺位（面板里根本没有）
当前面板只有 CAR + size/bm/leverage,论文回归还需要:
- ~~`momentum`、`volatility`~~ —— ✅ 已派生（2026-07-03，momentum=[-252,-21] 累计
  收益、volatility=估计窗年化标准差，覆盖 13,165/13,173 行）
- ~~AA 能力指标~~ —— ✅ 已对接（2026-07-03，`build_event_aa_metrics.py`：125 事件
  全部链接，88 个 LLM 旗舰（最高智能指数）+ 35 个媒体旗舰（最高 Elo）+ 2 个语音
  事件无可比指标；合并事件取旗舰的规则见 decisions/analysis_design_decisions.md T8）
- ~~事件标签~~ —— ✅ 已完成（2026-07-03，双盲编码：coder A/B 独立编 125 事件 ×8 维，
  总一致率 99.0%，κ 0.917~1.000；10 处分歧经硬证据核实 + 用户仲裁（SD3 定开源）；
  定稿 `decisions/event_label_decisions.csv`，已并入面板；过程留痕于 `标签/`）
- ~~关系编码~~ —— ✅ 已完成（2026-07-03）：双盲 A/B 编 2,700 对 ×8 维，
  一致率 99.0%（κ 0.839~1.000）；213 格分歧按 12 个公司级判断仲裁（以旧样本
  adjudicated 表为锚，多数从 B），定稿 `decisions/relationship_decisions.csv`，
  已并入面板（事件级 = 发布方并集）。R1 的 5 项扩展判定为 A/B 独立共识。
  注意：新公司池 R5（下游赋能）为空集（纳指100 无 IT 服务商），论文需说明。

### T9 首跑结果（2026-07-03，`Analysis/reports/t9_main_regressions.csv`）

主样本 12,112 行 / 124 事件 / 101 公司，SPY 市场模型，事件聚类 SE：
- **存活**：上游硬件溢价 +1.66pp CAR[0,20]（p=0.012；FF3 下 +1.79pp, p=0.007），
  方向与旧结论一致但幅度约为旧样本（+2.28pp）的七成；竞争者 +1.61pp（p=0.013）新浮现。
- **未复现**：下游部署负效应（旧 −1.90pp p<0.01 → 新 −0.07pp p=0.78）；
  上游硬件 × 开源交互（旧 p=0.018 → 新 p=0.897）；AA 智能指数不显著。
- **诊断**：限制到旧论文窗口（2024-04 后，102 事件）模式不变——差异非早期事件
  所致，而是可复现重建（新事件集/外生公司池/脚本化 CAR/前视修正）后旧结论不稳。
  与 [CAR_pre 控制会杀掉异质性结论] 的既有观察相互印证。

### P4. 主基准选择 —— ✅ 已拍板（2026-07-03）：主结果用 SPY
- QQQ/SOXX/FF3 降为稳健性；panel 报告已更新；决策留痕于
  `new data set/decisions/analysis_design_decisions.md`（连同 D1-D7、T6 暂缓、
  T7 保留媒体情绪一并记录）。D1 已实现为面板 `event_excluded_identity` 标记。

## 二、需要你拍板的规则（不阻塞数据，阻塞定稿）

| # | 事项 | 建议 |
|---|---|---|
| D1 | Grok 4.20（AIT-2026-02-006）事件身份三方矛盾（ATL 2 月/官方 3-10/AA 4-7），125 条里唯一 low 置信度 | 剔除，主样本 125→124 |
| D2 | 6 条多组件事件（SORA+o1、Opus/Sonnet 4.6 等，组件日期相差 4~12 天，晚组件污染 [0,+20] 窗口） | 保留 + "剔除这 6 条"稳健性 |
| D3 | 盘后/直播发布未解析（`release_time_status` 全 unknown，影响 t vs t+1） | 主结果维持现规则 + 全样本 t+1 平移稳健性 |
| D4 | 晚上市公司进入规则 | 维持现状（est_obs≥120 才估计），327 缺失行如实剔除 |
| D5 | CCEP/FER 半年报公司（财务季度缺一半） | 滞后匹配下 forward-fill 至多两个季度，超出置缺失 |
| D6 | 14 个交易日承载多个事件 + [0,+20] 窗口大面积重叠 | 沿用旧做法按事件聚类 SE；加事件固定效应稳健性；重叠是设计固有，论文里明说 |
| D7 | 估计窗：数据按 [-200,-10] 预留，论文旧稿写 [-120,-21] | 二选一写死进论文；建议沿用新的 [-200,-10]（已实现、数据足） |

## 三、欠缺的数据（要你拉或生产）

| 数据 | 用途 | 状态 |
|---|---|---|
| 媒体情绪（GDELT+FinBERT） | 论文"进一步分析"部分 | 你先拍板保不保留。保留则 108+ 新事件需重新生产,生产链在你那边;不保留则从论文删掉。建议二期后补,不阻塞主回归 |
| 历史成分股名单（可选） | 消除公司池幸存者偏差(现在用的是"当前"纳指100+SOX 名单) | 需付费数据源。若不补，论文注明以拉取日名单为准 + 剔除 2024 后新入指公司做稳健性 |
| 中概/港股子样本（可选） | 中国产业链对 DeepSeek 类事件的反应 | 上次讨论的遗留项，纯美股主样本不受影响 |

## 四、欠缺的计算（全部我可做，依赖上面的决策）

1. ~~momentum / volatility 派生~~ ✅ 已完成（2026-07-03）
2. ~~财务合并规则修正~~ ✅ 已完成（2026-07-03）
3. AA 指标 + 事件标签对接（P3/T8，无依赖，可立即做）
4. 关系编码初筛 + 分歧表（P3/T6，依赖你参与仲裁）
5. 面板 v2 合并（依赖 3-4 + D1-D5 决策）
6. 全部回归重跑 + 新旧样本结果对比报告（T9，依赖 5）

## 五、建议的执行顺序

```
你拍板: P4 基准 + D1~D7 （一次性说清即可）
   ↓
C 并行做: P1 修 join / P2 负BM / momentum·volatility / AA+标签  ← 无依赖，先动
   ↓
T6 关系编码: C 初筛 → 你仲裁分歧表   ← 关键路径上最大的人工工作量
   ↓
面板 v2 → T9 回归重跑 → 新旧结果对比
```

媒体情绪（T7）独立于以上全部，何时决定都不阻塞。
