# Old relationship archive

This folder collects files that were superseded by the current clean data and the new relationship analysis.

Current files kept in place:

- `data/panel/specr_rel_clean.csv`
- `data/panel/specr_input_clean.csv`
- `data/panel/clean_event_firm_panel.csv`
- `relationship_specr_newrel_report.md`
- `agent_tasks/relationship_specr_newrel_20260625-005125/`
- `output/paper_plan_core/`
- `Tex/long.tex` and `Tex/long.pdf`

Archived material includes:

- old raw relationship source files and backups
- transitional `specr_rel_clean_v2` and old backup copies
- old relationship result tables and specification-curve figures
- stale `output/data` copies
- stale `data/manifest.csv` inventory that referenced the archived old files
- old relationship task outputs from June 19 and the 6.21 VIX/news regression
- old task-level relationship CSVs used before the final adjudicated relationship switch

The archive is intended to be moved out of the project root if the current manuscript only uses the three clean panel files and the new relationship outputs.
